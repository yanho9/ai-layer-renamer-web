services:
  - type: web
    name: ai-layer-renamer-web
    env: python
    rootDir: backend
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
